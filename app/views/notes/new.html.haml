- content_for :title, "New Curator Note"

%ilw-content.dataset-show-page
  %section.idb-section-card{aria: { label: "New curator note" }}
    %details.idb-section-disclosure{open: true}
      %summary
        %h1.idb-section-title New Note
      .idb-section-body
        %p
          For
          = link_to @dataset.title, dataset_path(@dataset)
        = render "form"