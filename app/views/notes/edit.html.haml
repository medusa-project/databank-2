- content_for :title, "Edit Curator Note"

%ilw-content.dataset-show-page
  %section.idb-section-card{aria: { label: "Edit curator note" }}
    %details.idb-section-disclosure{open: true}
      %summary
        %h1.idb-section-title Edit Note
      .idb-section-body
        %p
          For
          = link_to @dataset.title, dataset_path(@dataset)
        = render "form"