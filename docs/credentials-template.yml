# Example structure for Rails credentials used by deployed environments.
#
# Edit with:
#   EDITOR=nano bin/rails credentials:edit --environment demo
#   EDITOR=nano bin/rails credentials:edit --environment production
#
# Do not commit decrypted credential contents.

app_url: "https://demo.databank.illinois.edu"
root_url_text: "https://demo.databank.illinois.edu"
system_user_email: "no-reply@databank.illinois.edu"

aws:
  region: "us-east-2"
  # Leave blank when using IAM role/profile auth.
  # access_key_id: ""
  # secret_access_key: ""

storage:
  draft_bucket: "example-draft-bucket"
  draft_prefix: "uploads/"
  medusa_bucket: "example-medusa-bucket"
  medusa_prefix: ""
  globus_bucket: "example-globus-bucket"
  globus_download_prefix: "download/"
  # Optional; falls back to storage.draft_bucket when omitted.
  # active_storage_bucket: "example-active-storage-bucket"

# Optional sections used only when corresponding feature flags are enabled.
datacite:
  # api_base_url: "https://api.test.datacite.org"
  # username: ""
  # password: ""
  # strict: true

ingest:
  # events_enabled: true
  # rabbitmq_url: "amqp://user:pass@host:5672/vhost"
  # events_exchange: "databank.ingest"
  # events_routing_key: "dataset.published"

globus:
  # transfer_enabled: true
  # transfer_endpoint: "https://globus.example.edu/api/transfers"
  # transfer_token: ""
  # source_collection: ""
  # destination_collection: ""
  # source_base_path: "/"
  # destination_base_path: "/"

core_curators:
  # emails: "curator1@example.edu,curator2@example.edu"

version_request:
  # review_emails: "reviewer@example.edu"

downloader:
  # endpoint: ""
  # user: ""
  # password: ""